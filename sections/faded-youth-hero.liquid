<style>
  /* === INLINE CSS FÜR FINALE HÖHEN- & BREITENKORREKTUR === */
  
  /* Container wird absolut positioniert, um Header-Margins zu ignorieren */
  .fy-hero-wrapper {
    position: absolute !important; /* Absolut, um über/unter den Flow zu gehen */
    top: 0 !important;             /* Zieht es ganz nach oben */
    left: 0 !important;
    right: 0 !important;
    
    height: 100vh !important;
    height: 100svh !important; /* iOS Safari Fix */
    width: 100% !important;
    
    margin: 0 !important;
    padding: 0 !important;
    background: #000 !important;
    overflow: hidden !important;
    z-index: 1 !important; /* Bleibt unter dem Header (z-index 99) */
  }

  .fy-hero-video {
    display: block !important;
    width: 100% !important;
    height: 100% !important;
    object-fit: cover !important; /* Füllt den Wrapper randlos */
    object-position: 50% 50% !important;
    background: #000 !important;
  }
  
  /* --- Mobile Zoom (65% Rauszoomen) --- */
  @media screen and (max-width: 768px) {
    .fy-hero-video {
      /* Skaliert das Video für den gewünschten "Rauszoomen"-Effekt */
      transform: scale(0.65) !important; 
      transform-origin: center center !important;
    }
  }

  /* WICHTIG: Setzt das Haupt-Content-Padding zurück, damit der Rest der Seite bündig anfängt */
  .content-for-index > section {
      padding-top: 0 !important;
      padding-bottom: 0 !important;
  }
</style>

{% comment %} 
Der Header (in header.liquid) muss einen z-index von 99+ haben, damit die Klicks funktionieren.
Das wurde in der base.css bereits implementiert.
{% endcomment %}

{% if template.name == 'index' %}
  <div class="fy-hero-wrapper">
    <video 
      class="fy-hero-video"
      autoplay
      muted
      loop
      playsinline
      preload="auto">
      
      <!-- Full HD -->
      <source src="https://cdn.shopify.com/videos/c/o/v/93b7c4458e9c49729d499191514b2502.mp4" type="video/mp4" media="(max-width:1280px)">
      
      <!-- 4K -->
      <source src="https://cdn.shopify.com/videos/c/o/v/646852d06d884d6e988af8f7629106db.mp4" type="video/mp4" media="(min-width:1281px)">
    </video>
  </div>
{% endif %}

<script>
document.addEventListener('DOMContentLoaded',()=>{
  const v=document.querySelector('.fy-hero-video');
  if(v){v.play().catch(()=>{v.muted=true;v.play();});}
});
</script>

{% schema %}
{
  "name": "FADED YOUTH Hero Video",
  "tag": "section",
  "class": "section fy-hero-section",
  "settings": [],
  "presets": [{ "name": "FADED YOUTH Hero Video" }]
}
{% endschema %}