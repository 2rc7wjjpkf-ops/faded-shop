{% comment %}
  FADED YOUTH — FINAL WHITE HEADER (STABLE BUILD)
  - Weißer Sticky Header
  - Logo zentriert
  - Menü links, Icons rechts
  - Schwarze Icons, keine Theme-Akzentfarbe
{% endcomment %}

<style>
  .section-header {
    position: relative;
    z-index: 300;
  }

  .fy-header {
    position: sticky;
    top: 0;
    left: 0;
    width: 100%;
    background: #fff;
    border-bottom: 1px solid #e5e5e5;
    z-index: 999;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .fy-header__inner {
    width: 100%;
    max-width: 1500px;
    margin: 0 auto;
    padding: 1.2rem 3rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  /* === NAVIGATION === */
  .fy-nav {
    display: flex;
    gap: 2.6rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .fy-nav a {
    color: #000;
    text-decoration: none;
    font-size: 1rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    font-weight: 500;
    transition: opacity 0.25s ease;
  }

  .fy-nav a:hover {
    opacity: 0.6;
  }

  /* === LOGO === */
  .fy-header__logo {
    display: flex;
    align-items: center;
    justify-content: center;
    flex: 1;
  }

  .fy-header__logo img {
    width: clamp(140px, 18vw, 240px);
    height: auto;
    object-fit: contain;
  }

  /* === ICONS === */
  .fy-icons {
    display: flex;
    align-items: center;
    gap: 1.6rem;
  }

  .fy-icons svg {
    width: 24px;
    height: 24px;
    fill: #000 !important; /* fix: override accent color */
    transition: opacity 0.25s ease;
  }

  .fy-icons svg:hover {
    opacity: 0.6;
  }

  /* === MOBILE === */
  @media (max-width: 768px) {
    .fy-header__inner {
      padding: 1rem 1.4rem;
    }
    .fy-nav {
      display: none;
    }
    .fy-header__logo img {
      width: 160px;
    }
  }
</style>

<section class="section-header">
  <header class="fy-header">
    <div class="fy-header__inner">

      <ul class="fy-nav">
        {% for link in linklists.main-menu.links %}
          <li><a href="{{ link.url }}">{{ link.title }}</a></li>
        {% endfor %}
      </ul>

      <a href="{{ routes.root_url }}" class="fy-header__logo">
        {% if settings.logo %}
          {{
            settings.logo
            | image_url: width: 600
            | image_tag:
              width: 200,
              alt: shop.name
          }}
        {% else %}
          <span style="color:black;">{{ shop.name }}</span>
        {% endif %}
      </a>

      <div class="fy-icons">
        <a href="{{ routes.search_url }}">
          <span class="svg-wrapper">{{ 'icon-search.svg' | inline_asset_content }}</span>
        </a>
        {% if shop.customer_accounts_enabled %}
          <a href="{{ routes.account_url }}">
            <span class="svg-wrapper">{{ 'icon-account.svg' | inline_asset_content }}</span>
          </a>
        {% endif %}
        <a href="{{ routes.cart_url }}">
          <span class="svg-wrapper">{{ 'icon-cart.svg' | inline_asset_content }}</span>
        </a>
      </div>

    </div>
  </header>
</section>

{% schema %}
{
  "name": "FADED YOUTH Header",
  "settings": [
    { "type": "image_picker", "id": "logo", "label": "Logo" }
  ]
}
{% endschema %}
