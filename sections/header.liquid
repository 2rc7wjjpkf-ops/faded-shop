{% comment %}
  FADED YOUTH — STABILER WEISSER HEADER (SHOPIFY SAFE)
  - Weißer Sticky Header mit Logo zentriert
  - Menü links, Icons rechts
  - Schwarze Icons, große Schrift
{% endcomment %}

<header class="header header--white" role="banner">
  <div class="header__inner page-width">
    <nav class="header__menu">
      <ul class="header__menu-list">
        {% for link in linklists.main-menu.links %}
          <li class="header__menu-item">
            <a href="{{ link.url }}" class="header__link">{{ link.title }}</a>
          </li>
        {% endfor %}
      </ul>
    </nav>

    <div class="header__logo">
      <a href="{{ routes.root_url }}">
        {% if settings.logo %}
          {{ settings.logo | image_url: width: 600 | image_tag: width: 200, alt: shop.name }}
        {% else %}
          <span class="header__text-logo">{{ shop.name }}</span>
        {% endif %}
      </a>
    </div>

    <div class="header__icons">
      <a href="{{ routes.search_url }}" class="header__icon">
        {{ 'icon-search.svg' | inline_asset_content }}
      </a>
      {% if shop.customer_accounts_enabled %}
        <a href="{{ routes.account_url }}" class="header__icon">
          {{ 'icon-account.svg' | inline_asset_content }}
        </a>
      {% endif %}
      <a href="{{ routes.cart_url }}" class="header__icon">
        {{ 'icon-cart.svg' | inline_asset_content }}
      </a>
    </div>
  </div>
</header>

<style>
  /* --- Basis --- */
  .header--white {
    background: #fff !important;
    border-bottom: 1px solid #e5e5e5;
    position: sticky;
    top: 0;
    z-index: 999;
  }

  .header__inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.4rem 2.8rem;
  }

  /* --- Menü --- */
  .header__menu-list {
    display: flex;
    gap: 2.6rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .header__link {
    color: #000 !important;
    text-decoration: none;
    font-size: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: opacity 0.25s ease;
  }

  .header__link:hover {
    opacity: 0.6;
  }

  /* --- Logo --- */
  .header__logo {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .header__logo img {
    max-width: 200px;
    height: auto;
  }

  /* --- Icons --- */
  .header__icons {
    display: flex;
    align-items: center;
    gap: 1.6rem;
  }

  .header__icon svg {
    width: 24px;
    height: 24px;
    fill: #000 !important; /* erzwingt schwarze Icons */
    transition: opacity 0.25s ease;
  }

  .header__icon:hover svg {
    opacity: 0.6;
  }

  /* --- Mobile --- */
  @media (max-width: 768px) {
    .header__menu {
      display: none;
    }

    .header__logo img {
      max-width: 160px;
    }
  }
</style>

{% schema %}
{
  "name": "FADED YOUTH Header",
  "settings": [
    { "type": "image_picker", "id": "logo", "label": "Logo" }
  ]
}
{% endschema %}
