{% comment %}
  FADED YOUTH HERO VIDEO SECTION
  Finaler Fix: Absolute Positionierung, um Header-Margin zu ignorieren und volle Höhe zu erzwingen.
{% endcomment %}

<style>
/* ABSOLUTE POSITIONIERUNG: Zwingt das Video, ganz oben anzufangen (ignoriert Header-Margin) */
.fy-hero-wrap {
  position: absolute !important;
  top: 0 !important;
  left: 0 !important;
  right: 0 !important;
  bottom: 0 !important;
  width: 100vw !important;
  height: 100vh !important;
  height: 100svh !important; /* iOS Safari Fix */
  overflow: hidden !important;
  z-index: 1 !important; /* Wichtig: MUSS HÖHER sein als Standard-Content, aber NIEDRIGER als Header (99999) */
}

/* Video-Element selbst */
.fy-hero-video {
  width: 100%;
  height: 100%;
  object-fit: cover !important;
  object-position: center center !important;
  display: block !important;
}

/* Header Klick-Fix: Nur nötig, falls der allgemeine z-index nicht greift */
.shopify-section-header, 
.header--top,
header.header {
  z-index: 99999 !important; /* MUSS über dem Video (1) liegen */
  position: relative !important; /* Nötig, damit z-index wirkt */
}


/* --- Mobile Zoom Out (65%) --- */
@media (max-width: 768px) {
  .fy-hero-wrap {
    /* Full width auf Mobile */
    width: 100vw !important;
    height: 100vh !important;
    height: 100svh !important;
    
    /* Mobile Ränder und Scroll verhindern */
    overflow-x: hidden !important;
    margin: 0 !important;
    padding: 0 !important;
  }
  
  .fy-hero-video {
    /* Skalierung und Positionierung für "Mann in der Mitte" */
    transform: scale(0.65) !important; 
    transform-origin: center center !important;
  }

  /* Globaler Reset für Body/HTML */
  body, html {
    overflow-x: hidden !important;
  }
}

</style>

{% if template.name == 'index' %}
  <div class="fy-hero-wrap">
    <video 
      class="fy-hero-video"
      autoplay
      muted
      loop
      playsinline
      preload="auto">
      
      <!-- Video Quellen, wie von dir bereitgestellt -->
      <source src="https://cdn.shopify.com/videos/c/o/v/93b7c4458e9c49729d499191514b2502.mp4" type="video/mp4" media="(max-width:1280px)">
      <source src="https://cdn.shopify.com/videos/c/o/v/646852d06d884d6e988af8f7629106db.mp4" type="video/mp4" media="(min-width:1281px)">
    </video>
  </div>
{% endif %}

<script>
document.addEventListener('DOMContentLoaded',()=>{
  const v=document.querySelector('.fy-hero-video');
  if(v){v.play().catch(()=>{v.muted=true;v.play();});}
});
</script>

{% schema %}
{
  "name": "FADED YOUTH Hero Video",
  "tag": "section",
  "class": "section",
  "settings": [],
  "presets": [{ "name": "FADED YOUTH Hero Video" }]
}
{% endschema %}
<style id="fy-hero-inline-fix">
  /* ===== FADED YOUTH — NUCLEAR HERO FIX ===== */

  /* 0) Falls der Wrapper eine ID fehlt, nimm #fy-hero ODER .fy-hero-wrap (beides wird abgedeckt) */
  /* Full-bleed ohne :has() – funktioniert auch in Safari, clippt Überbreite */
  #fy-hero, .fy-hero-wrap{
    position: relative !important;
    width: 100vw !important;
    margin: 0 !important;
    padding: 0 !important;
    /* zieht den Block aus jedem .page-width Padding raus → kein Rechts-Shift */
    margin-left: calc(50% - 50vw) !important;
    margin-right: calc(50% - 50vw) !important;
    overflow: hidden !important;
    background: #000 !important;
    z-index: 0 !important;
  }

  /* Video zentriert, kein Seitenscroll */
  #fy-hero video, .fy-hero-video{
    display: block !important;
    position: relative !important;
    left: 50% !important;
    transform: translateX(-50%) !important;
    width: 100vw !important;
    height: 100svh !important;     /* stabiler als 100vh */
    object-fit: cover !important;
    object-position: 50% 50% !important;
    background: #000 !important;
    z-index: 0 !important;
  }

  /* Mobile: mehr Szene sichtbar (~65% „rausgezoomt“), weiterhin mittig */
  @media (max-width: 768px){
    #fy-hero video, .fy-hero-video{
      width: 150vw !important;                /* bewusst breiter */
      height: 72svh !important;
      transform: translateX(-50%) scale(0.65) !important; /* ~65% Zoom-Out */
      transform-origin: center center !important;
      object-position: 50% 50% !important;
    }
  }

  /* Header bleibt über dem Video */
  .section-header, .header, .shopify-section-header{
    position: relative !important;
    z-index: 99 !important;
  }

  /* absolut hart: horizontales Scrollen global aus */
  html, body{ overflow-x: hidden !important; background:#000 !important; }
</style>